import React from 'react';
import {View} from 'react-native';
import * as THREE from 'three';
import {GLView} from 'expo-gl';

export default function ProteinDisplay() {
  //  const three = THREE.Scene();

  return (
    <View>
      <GLView
      //   onContextCreate={async gl => {
      //     console.log(gl);
      //   }
      // }
      />
    </View>
  );
}
